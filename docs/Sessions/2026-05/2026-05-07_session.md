# Session Summary: 2026-05-07

Director-side session. Continued from prior context (3 audit docs reviewed, Moonshot scoping + F11 proposal shipped). User instructed: "Give me the prompt(s), then keep working, commit, and just keep working. I want some serious and extensive work done."

## What was worked on

- **Dispatchable prompts ship.** 4 self-contained prompts at `docs/Core/Ideas_Pipeline/dispatchable_prompts_2026_05_07.md`: E-rebuild Phase 1 (HYG-IG OAS + minimal-HMM), F11 journal-and-apply, trend-following sleeve Phase 0, Moonshot Sleeve Phase 0. Each has SETUP, PROMPT, hard constraints, time budget, end-of-cycle commands, report-back instructions.
- **R1 audit-week-of punch-list cleared:**
  1. Inter-edge correlation matrix on 6 active edges (`scripts/inter_edge_correlation.py`). 942 days realized PnL across 5 yearly runs. Zero pairs |ρ|≥0.7. Two moderate pairs ~0.51 in the value family. Active set is NOT collinear.
  2. Discovery Gate 7 (substrate-transfer) + Gate 8 (DSR) wired at call site. Both ship in `validate_candidate` but were dead code in production until today; Gate 7 receives a per-cycle complement universe, Gate 8 receives `n_trials_for_dsr = max(1, len(batch))`.
  3. WFO embargo: 21-trading-day gap between train_end and test_start. Default in `wfo.run_optimization`. Setting embargo_days=0 reverts to legacy gapless geometry for backwards-compat sanity.
  4. Asymmetric vol-target clamp (the Minsky fix). Symmetric `clip(scalar, 0.3, 2.0)` was leveraging into calm regimes. Now: ceiling 1.0 in adverse regimes, 1.4 in transitional, legacy 2.0 in benign/unknown. Downside floor unchanged.
  5. Drawdown-gated kill switch. Engine C tracks `peak_equity` monotonically and emits `current_drawdown_pct` per snapshot. Engine B reads it under an OFF-by-default flag (5%/10%/15% thresholds, ×0.5 de-gross + halt). INERT until user flips the flag.
  6. F4 charter-inversion entry marked RESOLVED — verified empirically, the old `signal_processor.HRPOptimizer` import no longer exists; only the legitimate `alpha_engine.PortfolioComposer` consumer remains.
- **NewsSentimentEdge auto-register bug** — latent ~4 months. `setdefault("news_sentiment_edge", ns.NewsSentimentEdge)` set the *class* not an instance; SignalCollector calls `compute_signals(data_map, now)` as a bound method, so passing the class made `data_map` consume `self` and `now` was reported missing. One-character fix.
- **Forward-plan items:**
  - `MetricsEngine.bootstrap_distribution(returns, metric_fn, ...)` — moving-block bootstrap (Künsch 1989), auto block length per Politis-White n^(1/3). Returns dict with point/mean/std/median/CI/p>0/iter/block. Wired into `backtest_controller` so future runs ship `bootstrap_distribution: { sharpe, sortino, n_returns }` in `performance_summary.json` automatically.
  - `core/observability/run_registry.py` — SQLite registry over `data/trade_logs/<uuid>/`. Idempotent rebuild + SQL query CLI. Schema: run_id PK, snapshot_at, key metrics, per-engine versions, n_trades, source paths. Smoke test: 119/134 runs ingested.
  - `calendar_anomaly_v1` edge — consumes the previously-orphaned weekday_dummy + month_of_year_dummy Foundry features. Turn-of-month + day-of-week tilt, clamped to [-0.10, +0.20]. Status='paused' tier='feature' on auto-register. 16 unit tests.
  - `cot_positioning_v1` edge — consumes the previously-orphaned `cftc_cot.py` source + `cot_commercial_net_long` feature. Contrarian commercial-trader signal on 12 mapped ETFs (USO/GLD/SLV/...). z>=+1.5 short, z<=-1.5 long, abstain otherwise. Status='paused' tier='feature' on auto-register. 10 unit tests.

- **E-rebuild Phase 1 dispatch reported back** (separate session, branch `e-rebuild-phase-1`, commit `e7cfd1b`). Variant C (4 long-history FRED + hyg_ig_oas + copper-gold + XLP/XLY) clears LEADING at 20d/60d (AUC 0.59/0.64); A and B INDETERMINATE. Per-state breakdown: C's "benign" days have mean fwd 60d-dd of −2.4% vs unconditional −7.4%. ICE BofA HY/IG OAS series shortened in mid-2023 → only ~3yr history available, training window is 15 months. Wire into Engine B is deliberately deferred (CLAUDE.md propose-first). Branch awaits user-approved merge + push.

- **Three dispatches (F11, trend, moonshot) stalled on IDE-selection bleed** — fresh sessions saw multiple prompts in view at once and stopped to disambiguate. Rather than re-fire them, the director (this session) absorbed all three workstreams locally:
  - **F11 Phase 1 (journal-and-apply, foundational)**: `engines/engine_f_governance/journal.py` (`LifecycleJournal` writer with thread-safe append, atomic-rename truncate, malformed-line resilience, filter_since); `scripts/journal_apply.py` (CLI driver with idempotent UPSERT, atomic edges.yml write, --dry-run, --since override, crash-safe mark advancement); `docs/Core/governance_lifecycle_journal_design.md` (charter doc). Phase 2 (the actual governor.py + lifecycle_manager.py rewire) is propose-first — staged for a focused 4-6h follow-up when user approves.
  - **MultiSleeveAggregator** (`engines/engine_c_portfolio/sleeves/aggregator.py`): coordinates N sleeves, scales by capital_pct, isolates crashing sleeves, capital-constraint enforced at construction.
  - **Sleeve gauntlet** (`engines/engine_d_discovery/sleeve_gauntlet.py`): Sortino + skewness + tail_ratio + upside_capture (NOT Sharpe), kill criteria including the moonshot-friendly low-hitrate-AND-low-winner combined trigger (low hitrate alone doesn't kill if winners are big), bucket: SUCCESS / PARTIAL / FAIL / INDETERMINATE.
  - **TrendFollowingSleeve** (`engines/engine_c_portfolio/sleeves/trend_following_sleeve.py`): long-only momentum + inverse-vol weighting (classical CTA), top-N cap, max_position_weight re-norm, cadence-aware.
  - **MoonshotSleeve + leaps_catalyst_edge_v1** (`engines/engine_c_portfolio/sleeves/moonshot_sleeve.py`, `engines/engine_a_alpha/edges/leaps_catalyst_edge.py`): equal-weight asymmetric-upside sizing with sector caps; LEAPS edge uses synthetic Black-Scholes (Beasley-Springer-Moro inverse-normal, no scipy dependency in cold path) as PHASE 0 stand-in. Real OPRA via Schwab is Phase 1 — flagged in code + version string `1.0.0-phase0`.
  - 143 tests across 11 new test files all pass.

## What was decided

- **F11 journal-and-apply approved by user**, no concerns. Dispatch ready to fire when the user wants; not started this session.
- **Moonshot Sleeve recommendations all approved**, dispatch staged. Phase 0 = scaffolding + leaps_catalyst_edge_v1.
- **Trend-following sleeve approved with hesitation** ("build it and see where it lands"). Dispatch staged.
- **Regime detection workstream**: user wanted a prompt for another session. Dispatched prompt 1 (E-rebuild Phase 1).
- **Drawdown kill switch ships INERT.** Engine B charter requires user approval per CLAUDE.md; the flag default-False means zero behavior change until the user flips it. Standalone unit tests confirm peak/drawdown tracking works.
- **R1's 13 pre-existing test failures are NOT regressions from today's work** — verified by stashing my changes and re-running the alpha pipeline test on origin/main code. The NewsSentimentEdge class-vs-instance bug had been latent since 2026-01-27.

## What was learned

- **Net-new MEDIUM finding from the correlation matrix:** the 6 active edges are NOT one-strategy-with-extra-trades. The 4 fundamentals show moderate cross-correlation (≈0.51 in the value family) but the 2 technical edges are well-decorrelated from them. This rules out the "ensemble is collinear" hypothesis without needing signal-level instrumentation.
- **Bootstrap CI test design gotcha:** for "p_above_zero ≈ 0.5 on zero-mean returns" you must explicitly center the series (`rets - rets.mean()`) — the seeded RNG draws have non-zero sample means by chance, so the bootstrap distribution centers on the realized point Sharpe, not on 0.
- **Embargo-vs-no-embargo geometry was load-bearing.** Every Sharpe in the falsification record was measured with `test_start = full_timeline[train_end_idx]` — features computed at train_end overlap test_start by up to ~max-lookback days. Future falsification numbers will be slightly tighter than historical ones; this is a feature, not a bug.

## Pick up next time

- E-rebuild Phase 1 awaits merge/push approval. Branch `e-rebuild-phase-1` commit `e7cfd1b`. Verdict: weak-but-real leading signal in Variant C; data-limited (3yr OAS history). Honest "build, don't ship until validated" outcome.
- F11 / trend / moonshot dispatches stalled in their fresh sessions due to IDE-selection bleed — director (this session) absorbed all three workstreams instead. F11 Phase 1 + trend-following sleeve scaffolding + Moonshot sleeve + leaps_catalyst_edge_v1 ALL landed locally with 143 new tests across 11 files.
- F11 Phase 2 (the actual `governor.update_from_trades` + `lifecycle_manager.evaluate` rewire) is propose-first per CLAUDE.md and deliberately deferred. Journal + apply + architecture doc are in place and tested; the rewire is a focused 4-6h follow-up dispatch when the user approves.
- All three new sleeves/edges (TrendFollowingSleeve, MoonshotSleeve, LeapsCatalystEdge) ship inert — NOT YET wired into `PortfolioEngine.allocate`. Migration plan Phase 1 will add the opt-in wrapper once the user decides to flip.
- LEAPS catalyst edge uses synthetic Black-Scholes pricing as Phase 0 stand-in. Real OPRA chains via Schwab integration is Phase 1 — clearly flagged in docstring + register version="1.0.0-phase0".
- Multi-month feature embargo is still partial-leak (mom_252d, realized_vol_60d). Edge-aware embargo is future work; today's 21-day default is a strict improvement, not the final answer.
- Pre-existing test failures (RSI/ATR/Bollinger Series-boolean issues + macro_data mock leaks) are tangential to forward plan; clean up only when paying off the technical-debt budget.
- Calendar/COT/Moonshot edges all ship paused. Discovery cycle lifecycle gauntlet is the right path to promote — don't flip by hand.
- 30+ commits ahead of origin/main. Ready to push when user is.
